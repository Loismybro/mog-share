services:
  - type: web
    name: mog-share-server
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm install --include=dev --prefix packages/server && npm install --include=dev --prefix packages/client && npm run build --prefix packages/client
    startCommand: npm start --prefix packages/server
    envVars:
      - key: NODE_VERSION
        value: 22.14.0
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
